{
  "name": "everlume",
  "private": true,
  "description": "Everlume — premium research catalog and customer platform (static Netlify site + Supabase backend).",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node validate.mjs"
  }
}
